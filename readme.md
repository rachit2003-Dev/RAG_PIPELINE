# 🚀 Smart Validation API

A **containerized FastAPI service** that validates **Indian GST numbers** and **US addresses** using both **regex** and **OpenAI-powered intelligent checks**.

This project is designed to be **client-ready**, with automatic dependency handling and Docker compatibility.

---

## 🧩 Features

- ✅ Smart GST number validation (regex + AI cross-check)
- ✅ Intelligent US address validation (ZIP check + OpenAI logic)
- ✅ Automatic requirements installation if missing
- ✅ `.env` based configuration
- ✅ Fully containerized with Docker support
- ✅ Easy local run with no manual setup needed

---

## ⚙️ Setup Instructions

### 1️⃣ Clone the Repository
```bash
git clone https://github.com/yourrepo/smart-validation-api.git
cd smart-validation-api